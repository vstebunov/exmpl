import { render, screen } from '@testing-library/react';
import App from './App';

/*
test('List of manufacturers on first page', () => {
    render(<App />);
    const listOfManufacturers = screen.getByText(/List of Manufacturers/i);
    expect(listOfManufacturers).toBeInTheDocument();
});
*/
//TODO: detail
